<Window x:Class="WpfHomeWorksTask7.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfHomeWorksTask7"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="500">
    <Window.Resources>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Rectangle x:Name="Rectangle" 
                                       RadiusX="3" RadiusY="3" 
                                       Grid.Row="0" 
                                       Stroke="Gray" 
                                       Width="20" Height="20" 
                                       VerticalAlignment="Bottom"/>
                            <ContentPresenter Content="{TemplateBinding Content}" 
                                              Grid.Row="1" 
                                              VerticalAlignment="Top" HorizontalAlignment="Center"/>
                            <Ellipse x:Name="Ellipse" 
                                     Grid.Row="0"
                                     VerticalAlignment="Bottom"
                                     Margin="5"
                                     Width="10" Height="10"
                                     Fill="Gray"
                                     Visibility="Hidden"/>
                            <TextBlock x:Name="X" 
                                       Text="X"
                                       Grid.Row="0"
                                       VerticalAlignment="Bottom" HorizontalAlignment="Center"
                                       Foreground="Gray" FontWeight="Bold" FontSize="16"
                                       Visibility="Hidden"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="X" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="{x:Null}">
                                <Setter TargetName="Ellipse" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <CheckBox Grid.Column="0" Content="UnChecked" IsChecked="False"/>
        <CheckBox Grid.Column="1" Content="Checked" IsChecked="True"/>
        <CheckBox Grid.Column="2" Content="Indeterminate" IsChecked="{x:Null}"/>
    </Grid>
</Window>
