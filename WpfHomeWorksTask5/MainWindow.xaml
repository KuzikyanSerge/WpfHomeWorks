<Window x:Class="WpfHomeWorksTask5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfHomeWorksTask5"
        mc:Ignorable="d"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="Панель настроек" Height="500" Width="400" MinHeight="350" MinWidth="350">
    <Window.Resources>
        <Style x:Key="InteractiveButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Style.Setters>
                <Setter Property="Padding" Value="10"/>
            </Style.Setters>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource HoverColor}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{StaticResource PressedColor}"/>
                </Trigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsMouseOver" Value="True"/>
                        <Condition Property="IsEnabled" Value="False"/>
                    </MultiTrigger.Conditions>
                    <Setter Property="Background" Value="{StaticResource DisabledColor}"/>
                </MultiTrigger>                
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock
            Grid.Row="0"
            Text="Настройки приложения"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            Margin="10,10"
            Style="{StaticResource HeaderTextStyle}"/>
        <Button
            Grid.Row="1"
            Content="Сохранить"
            Style="{StaticResource InteractiveButtonStyle}"/>
        <Button
            Grid.Row="2"
            Content="Отменить"
            Style="{StaticResource InteractiveButtonStyle}"/>
        <CheckBox 
            x:Name="ToggleButton"
            Grid.Row="3"
            Content="Включить режим" 
            VerticalAlignment="Center"
            Margin="10,10"/>
        <Button
            Grid.Row="4"
            Content="Специальная кнопка"
            Style="{StaticResource InteractiveButtonStyle}"
            IsEnabled="{Binding IsChecked, ElementName=ToggleButton}"/>
    </Grid>
</Window>
