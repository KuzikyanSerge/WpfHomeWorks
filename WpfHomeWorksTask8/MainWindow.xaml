<Window x:Class="WpfHomeWorksTask8.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfHomeWorksTask8"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate x:Key="ProductDataTemplate">
            <Border x:Name="border" Padding="10"
                    CornerRadius="10" 
                    Background="#FF9FFF8B">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding imya}" />
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding tsena}" />
                    <Image Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Width="100" Height="100"
                        Source="{Binding izobradzenie}" />
                </Grid>
            </Border>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding tipTovara}">
                    <DataTrigger.Value>
                        <local:TipTovara>Бытовая_техника</local:TipTovara>
                    </DataTrigger.Value>
                    <Setter TargetName="border" Property="Background" Value="#FF8ED3FF" />
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <ListBox Name="lstBox" 
                 Margin="10" 
                 HorizontalContentAlignment="Stretch" 
                 ItemTemplate="{StaticResource ProductDataTemplate}" />
    </Grid>
</Window>
